import React from 'react';
import RN from '../../components/RN';

const MarketScreen = () => {
  return (
    <RN.View>
      <RN.Text>MarketScreen</RN.Text>
    </RN.View>
  );
};

export default MarketScreen;

const styles = RN.StyleSheet.create({});
