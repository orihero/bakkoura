import React from 'react';
import RN from '../../components/RN';

const ToDoTimer = () => {
  return (
    <RN.View>
      <RN.Text>ToDoTimer</RN.Text>
    </RN.View>
  );
};

export default ToDoTimer;

const styles = RN.StyleSheet.create({});
